# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

OPTIONS        /*all                                  be.cafeba.cors.controllers.CorsController.preflight(all: String)
